'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { api, apiUrl, bidFromToken } from '../../lib/api';
export default function AdminLogin() { const router=useRouter(); const [username,setUsername]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); async function submit(e:FormEvent){e.preventDefault();try{const response=await fetch(`${apiUrl}/api/business/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username,password})});if(!response.ok)throw new Error('Invalid username or password');const {accessToken}=await response.json();const bid=bidFromToken(accessToken);const stores=await api<{id:string}[]>(`/api/business/${bid}/stores`,accessToken);if(!stores[0])throw new Error('No store is available');localStorage.setItem('digitize_token',accessToken);router.push(`/business/${bid}/store/${stores[0].id}/dashboard`)}catch(err){setError(err instanceof Error?err.message:'Unable to sign in')}} return <main className="auth"><h1>Digitize Admin</h1><form onSubmit={submit}><label>Username or email</label><input value={username} onChange={e=>setUsername(e.target.value)} required/><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/><p><button>Sign in</button></p>{error&&<p className="error">{error}</p>}</form></main>; }
